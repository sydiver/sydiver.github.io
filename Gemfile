source "https://rubygems.org"

# For local preview (optional). GitHub Pages builds automatically.
gem "github-pages", group: :jekyll_plugins

# Needed for local build when using `remote_theme:` in _config.yml.
gem "jekyll-remote-theme", group: :jekyll_plugins
